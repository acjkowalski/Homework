?readLines
?scan
3 < 4
sleep
sleep$extra > 0
which(sleep$extra > 0)
sleep$group == 1
sleep[which(sleep$gorup ==1), ]
sleep[which(sleep$group ==1), ]
which(sleep$group ==1)
sleep[sleep$group == 1 & sleep$extra > 0 , ]
sleep[sleep$group == 1 | sleep$extra > 0 , ]
sleep[!sleep$group == 1, ]
sleep[sleep$ID %in% c(1,3,4,6), ] # if in this set of things, returns TRUE, if not, returns FALSE
sleep[sleep$ID %in% c(1,3,4,6), ] # if in this set of things, returns TRUE, if not, returns FALSE
sleep[sleep$ID == c(1, 3, 4, 6)] # DOESNT WORK
sleep[sleep$ID %in% c(1,3,4,6), ] # if in this set of things, returns TRUE, if not, returns FALSE
X <- c(1, 2, NA, 5)
x > 3
x <- c(1, 2, NA, 5)
x > 3
subject.info <- data.frame(ID = 1:10, gender = c("male","female","male","male","female","female","female","female","male","female"))
sleep.merged <- merge(sleep, subject.info)
sleep
sleep.merged
subject.info <- data.frame(ID = 1:10, gender = c("male","female","male","male","female","female","female","female","male","female", "male","male"))
subject.info <- data.frame(ID = 1:12, gender = c("male","female","male","male","female","female","female","female","male","female", "male","male"))
sleep.merged2 <- merge(sleep, subject.info, all=TRUE)
mysleep <- sleep
mysleep$ID <- as.numeric(mysleep$ID)
sleep.merged2 <- merge(mysleep, subject.info, all=TRUE)
sleep.merged2
nrow(sleep)
nrow(sleep.merged)
mysleep
mysleep$Study <- "Study1"
mysleep$noise <- rnorm(20)
mysleep
